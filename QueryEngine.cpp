//changeWord takes a string called word as its argument and sets the class variable Word to this value, then calls the function stemWord
//function stemWord ges the stemWord of the word stoed in the class varibale word it first trims this word by removing any non alphabetic charccter and converts all alphabetsic character sto lowercase using the porter libarry
